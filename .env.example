# ==========================================
# HOSPITAL OVER DUTY RECORD MANAGER
# Production & Development Environment Template
# ==========================================

# Backend Configuration
PORT=5000
NODE_ENV=production
MONGODB_URI=mongodb://127.0.0.1:27017/over_duty_db
JWT_SECRET=replace_with_a_secure_random_production_secret_key_at_least_32_characters
JWT_EXPIRES_IN=7d
CORS_ORIGIN=https://yourdomain.com

# Frontend Configuration (When deploying frontend independently)
# VITE_API_BASE_URL=https://yourdomain.com/api
